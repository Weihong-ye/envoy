network: added experimental Linux UBSocket v2 segment-API integration with startup capability
validation, fixed 64 KiB blocks, bounded reads and controlled preload initialization. This development
integration requires a matching UBSocket v2 provider and does not fall back to TCP for selected UB addresses.
